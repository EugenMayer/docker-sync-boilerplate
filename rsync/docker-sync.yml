version: "2"

syncs:
  #IMPORTANT: ensure this name is unique and does not match your other application container name
  appcode-rsync-sync: #tip: add -sync and you keep consistent names als a convention
    src: './app folder/'
    dest: '/var/www'
    sync_host_ip: 'localhost'
    sync_host_port: 10872
    sync_strategy: 'rsync'

